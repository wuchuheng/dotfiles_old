#!/bin/bash

# zsh cli test.
function zsh_installed_test() {
    echo "zsh test after installed."
}

handle_testing_callback "zsh_installed_test" "the integration test of the zsh cli after the dotfiles installed."

